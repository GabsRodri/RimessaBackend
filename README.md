# Rimessa Backend 🚚

Backend del proyecto Rimessa, desarrollado en Spring Boot con conexión a PostgreSQL (Neon).

## Configuración local
1. Copia `application-example.properties` a `src/main/resources/application.properties`
2. Edita con tus credenciales de base de datos Neon.
3. Ejecuta:
   ```bash
   mvn spring-boot:run
